apiVersion: v1
kind: Service
metadata:
  namespace: solidserver
  annotations:
      alb.ingress.kubernetes.io/target-type: ip
  name: test-service
spec:
  selector:
    app: solidserver
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
  type: NodePort
